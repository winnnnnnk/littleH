# XiaoH 共享语言

本文记录 XiaoH 4.0.0 全面运行时重构已经确认的核心术语。需求、设计、代码、测试和后续排障应使用相同含义。

| 术语 | 类型 | 定义 | 反例或易混点 | 来源 | 状态 |
|---|---|---|---|---|---|
| 代码候选版本 | 状态 | 已通过仓库隔离验收、但尚未安装本机的 XiaoH 版本 | 不等于已部署或已激活 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 用户长期资产 | 领域名词 | 跨运行时版本仍属于用户的内容、映射和偏好 | 不包括旧 Hook、绑定和事务状态 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 受管运行时 | 领域名词 | 由 XiaoH 版本完整部署和验证的 Agent、Hook、Validator、模板及配置块 | 不等于插件缓存或用户笔记 | 当前产品 | 已确认 |
| 安装计划 | 领域名词 | 第一次写入前形成的资产、冲突、预计写入和恢复条件 | 不等于事务实际写入 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 运行时候选 | 领域名词 | 在隔离目录构建并完成静态验证的待部署内容 | 不等于当前有效运行时 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 恢复清单 | 领域名词 | 记录备份项、摘要、来源和恢复目标的证据 | 不等于普通目录副本 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 补偿事务 | 领域名词 | 通过分阶段日志和反向操作恢复跨根目录一致性的事务 | 不等于单文件系统原子事务 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 激活状态 | 状态 | 文件部署后，当前 Codex 进程是否已重启、信任并加载运行时 | 不等于安装脚本退出状态 | 用户历史反馈 | 已确认 |
| 完整诊断 | 业务动作 | 一次收集全部安全可执行的独立诊断 | 不等于首错退出 | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 显式代码评审 | 业务动作 | 用户主动请求、绑定固定 Diff、只执行一次的代码评审 | 不等于默认门禁或 Review Campaign | XiaoH 4.0.0 Spec+RFC | 已确认 |
| Manifest 驱动治理 | 边界规则 | 第三方 Skill 来源与决策由数据清单描述，校验器通用执行 | 不等于运行时代码硬编码每项 Skill | XiaoH 4.0.0 Spec+RFC | 已确认 |
| 执行通道 | 领域名词 | 根据任务风险选择的 `fast`、`standard` 或 `high_risk` 实现与验证深度 | 不等于需求工件路由或 Agent 权限 | XiaoH 4.0.0 Spec+RFC 修订 3 | 已确认 |
| 直接改动 | 业务动作 | 低风险、局部、可逆、无新增持久语义且可由影响面验证证明的代码改动 | 不等于跳过测试或规避需求确认 | XiaoH 4.0.0 Spec+RFC 修订 3 | 已确认 |
| 影响面验证 | 业务动作 | 从实际改动接口、调用方、契约和依赖推导检查集合 | 不等于随意少跑测试 | XiaoH 4.0.0 Spec+RFC 修订 3 | 已确认 |
| 完整失败集 | 状态 | 首轮完成全部独立安全检查后按共同根因归组的一次性失败集合 | 不等于每修一个问题就重新全量评审 | XiaoH 4.0.0 Spec+RFC 修订 3 | 已确认 |
| 根执行绑定 | 授权证据 | 根任务在副作用前将当前任务上下文、允许路径、仓库基线和有效期绑定成的短时授权 | 不等于长期权限，也不授权语义或范围扩大 | XiaoH 4.0.0 Spec+RFC 修订 4 | 已确认 |
| 范围证明 | 验证证据 | 依据执行绑定前后 Git 状态和文件摘要得出的实际改动清单及越界结论 | 不等于模型自述的 changed_scope | XiaoH 4.0.0 Spec+RFC 修订 4 | 已确认 |
| 验证证据 | 验证证据 | 与任务验证 ID、命令、时间、退出码和日志摘要绑定的可核验执行结果 | 不等于只有 `passed` 字符串的运行记录 | XiaoH 4.0.0 Spec+RFC 修订 4 | 已确认 |

## 使用规则

- “安装完成”只描述文件部署结果，不能替代激活状态。
- “完整诊断”要求一次聚合全部可安全执行的诊断，不表示 Doctor 可以自动修复。
- “显式代码评审”只能由用户主动触发，不能成为默认实现门禁。
- “用户长期资产”可以跨版本导入，但旧受管运行时和旧授权证据不能随之继承。
- “快速通道”仍要求影响面验证；发现新语义或风险信号时必须升级。
- “执行通道”只决定实现和验证深度，不授权 Agent，也不替代需求工件。
