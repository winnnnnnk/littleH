interface:
  display_name: "Spec/OpenSpec Consistency"
  short_description: "核对OpenSpec是否完整承接已确认Spec+RFC"
  default_prompt: "使用 $spec-rfc-openspec-consistency-review 比对当前 Spec+RFC 与 OpenSpec 的覆盖和语义一致性。"

policy:
  allow_implicit_invocation: true
